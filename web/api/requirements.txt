# Pinned for the local web API (install into the project venv or a dedicated venv).
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17
httpx==0.28.1

# Dev / tests
pytest==8.3.4
pytest-asyncio==0.24.0
